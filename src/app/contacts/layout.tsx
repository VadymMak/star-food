import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Us — Get a Quote for Wholesale Food Products",
  description:
    "Contact UB Market LTD for wholesale sunflower oil pricing, product samples, and partnership inquiries. Based in Varna, Bulgaria. We respond within 24 hours.",
};

export default function ContactsLayout({ children }: { children: React.ReactNode }) {
  return children;
}